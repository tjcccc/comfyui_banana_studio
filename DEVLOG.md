# DEVLOG

## 0.6.0 - 2026-04-21

- added local `config.ini` support for `GEMINI_API_KEY`
- kept the node `api_key` input as a fallback for temporary testing
- added `config.ini.example` and ignored real `config.ini`
- replaced the old frontend-template README with project-specific documentation
